groups:
    - name: default
      rules:
        - alert: ee
          expr: up == 1
          labels:
            severity: WARNING
            tenant: default
          annotations:
            description: ee
